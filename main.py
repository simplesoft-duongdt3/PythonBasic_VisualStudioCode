# First time
# create virual enviroment
# py -3 -m venv .venv_basic_python
# Ctrl + Shift + P -> Python: Select -> .venv_basic_python
# Ctrl + Shift + P -> Terminal: Create New Integrated Terminal
# Error cannot be loaded because running scripts is disabled on this system. For more information, see about_Execution_Policies
# Change to Powershell: Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
# pip install numpy

# Everytime open project
# Ctrl + Shift + P -> Terminal: Create New Integrated Terminal

# Pylance extension
# "python.languageServer": "Pylance"

# Git
#    Open the command line.
#    Set your username: git config --global user.name "FIRST_NAME LAST_NAME"
#    Set your email address: git config --global user.email "MY_NAME@example.com"
import numpy

msg = "Hello world!!"

print(msg)